# notification-service runs on port 8086
server.port=8086
spring.application.name=notification-service

# MySQL — own schema
spring.datasource.url=jdbc:mysql://localhost:3306/connectsphere_notification?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=divyansh
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT must match all other services exactly
jwt.secret=connectsphere_super_secret_key_change_this_in_production_min_32_chars

# Spring Mail  configure your SMTP credentials here
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=bhatt11divyansh@gmail.com
spring.mail.password=aoln vpgl mwle lvmd
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# Set to false in prod to actually send emails; true suppresses sending
spring.mail.test-connection=false

# Notification-service has no outbound inter-service REST calls.
# Other services CALL notification-service (fire-and-forget POST requests).

logging.level.com.connectsphere=DEBUG
