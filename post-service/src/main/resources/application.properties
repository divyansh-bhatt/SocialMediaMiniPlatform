# Server post-service runs on port 8082
server.port=8082

spring.application.name=post-service

# MySQL separate DB schema from auth-service
spring.datasource.url=jdbc:mysql://localhost:3306/connectsphere_post?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=divyansh
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT MUST match the secret in auth-service exactly
jwt.secret=connectsphere_super_secret_key_change_this_in_production_min_32_chars

# Inter-service URL  post-service calls auth-service to validate tokens
auth.service.url=http://localhost:8081

follow.service.url=http://localhost:8085

# Logging
logging.level.com.connectsphere=DEBUG

cloudinary.cloud_name=dyi8qdv3p
cloudinary.api_key=917714253593674
cloudinary.api_secret=drL1tBbp1nvRgGGkCPNk0AmEDbM
search.service.url=http://localhost:8088
